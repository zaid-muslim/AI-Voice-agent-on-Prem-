"""Banking domain pack - the second proof point for domain_agent_core's
core thesis: a genuinely different vertical (different tools, different
safety rules, different compliance profile) running on the exact same
``core/`` runtime as the hospital pack, with zero shared code beyond
``core/``.
"""

from __future__ import annotations
